import { ChevronDirective } from './chevron.directive';

describe('ChevronDirective', () => {
  it('should create an instance', () => {
    const directive = new ChevronDirective();
    expect(directive).toBeTruthy();
  });
});
